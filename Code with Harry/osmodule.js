const os = require('os'); //this is built-in module so do not need to give specifies path with ".(dot)".
console.log(os.freemem()); //free memory
console.log(os.homedir()); //home directory
console.log(os.type()); //FILE TYPE